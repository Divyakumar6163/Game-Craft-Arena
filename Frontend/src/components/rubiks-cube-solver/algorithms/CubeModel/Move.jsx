export default class Move {
  constructor(
    name,

    axis,

    layer,

    clockwise,
  ) {
    this.name = name;

    this.axis = axis;

    this.layer = layer;

    this.clockwise = clockwise;
  }
}
